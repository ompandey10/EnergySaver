# System Architecture & Connection Guide

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     EnergyFlow Application                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                       CLIENT LAYER                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              React Frontend (Port 5173)                    │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │                                                             │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │  │
│  │  │ Login    │  │Dashboard │  │  Homes   │  │ Devices  │   │  │
│  │  │  Page    │  │   Page   │  │   Page   │  │  Page    │   │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │  │
│  │       │             │             │             │          │  │
│  │       └─────────────┴─────────────┴─────────────┘          │  │
│  │                      │                                      │  │
│  │              ┌───────▼────────┐                            │  │
│  │              │ React Router   │                            │  │
│  │              │   (Routing)    │                            │  │
│  │              └───────┬────────┘                            │  │
│  │                      │                                      │  │
│  │              ┌───────▼────────┐                            │  │
│  │              │ Auth Context   │                            │  │
│  │              │ (User State)   │                            │  │
│  │              └───────┬────────┘                            │  │
│  │                      │                                      │  │
│  │              ┌───────▼────────────────┐                    │  │
│  │              │   API Services Layer   │                    │  │
│  │              │  (authService,         │                    │  │
│  │              │   homeService, etc)    │                    │  │
│  │              └───────┬────────────────┘                    │  │
│  │                      │                                      │  │
│  │              ┌───────▼────────┐                            │  │
│  │              │  Axios Client  │                            │  │
│  │              │ (HTTP Requests)│                            │  │
│  │              └───────┬────────┘                            │  │
│  │                      │                                      │  │
│  └──────────────────────┼──────────────────────────────────────┘  │
│                         │                                          │
│                    HTTP │ Port 5173 -> 3001                       │
│                         │                                          │
└─────────────────────────┼──────────────────────────────────────────┘

                          │
                   ┌──────▼──────┐
                   │ CORS Enabled│
                   └──────┬──────┘
                          │
┌─────────────────────────▼──────────────────────────────────────────┐
│                     SERVER LAYER                                  │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │          Express.js Backend (Port 3001)                     │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │                                                              │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │            Request Flow                               │ │ │
│  │  │                                                        │ │ │
│  │  │  1. Request arrives at Route                          │ │ │
│  │  │  ↓                                                     │ │ │
│  │  │  2. Auth Middleware (JWT Verification)               │ │ │
│  │  │  ↓                                                     │ │ │
│  │  │  3. Validation Middleware                             │ │ │
│  │  │  ↓                                                     │ │ │
│  │  │  4. Controller Logic                                  │ │ │
│  │  │  ↓                                                     │ │ │
│  │  │  5. Database Query (Mongoose Model)                   │ │ │
│  │  │  ↓                                                     │ │ │
│  │  │  6. Response to Client                                │ │ │
│  │  │                                                        │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  │                                                              │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │ │
│  │  │ Auth Routes  │  │Home Routes   │  │Device Routes │  ...  │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘       │ │
│  │         ↓                 ↓                 ↓                 │ │
│  │  ┌──────────────────────────────────────────────────────┐   │ │
│  │  │           Controllers                               │   │ │
│  │  │  (authController, homeController, etc.)             │   │ │
│  │  └──────────────┬───────────────────────────────────────┘   │ │
│  │                 │                                            │ │
│  │                 ↓                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐   │ │
│  │  │         Database Models                              │   │ │
│  │  │ (User, Home, Device, Reading, Alert, etc.)          │   │ │
│  │  └──────────────┬───────────────────────────────────────┘   │ │
│  │                 │                                            │ │
│  └─────────────────┼────────────────────────────────────────────┘ │
│                    │                                              │
│              ┌─────▼─────┐                                        │
│              │ Mongoose  │                                        │
│              │   ODM     │                                        │
│              └─────┬─────┘                                        │
│                    │                                              │
└────────────────────┼──────────────────────────────────────────────┘

                     │
            ┌────────▼────────┐
            │ TCP Connection  │
            │ localhost:27017 │
            └────────┬────────┘
                     │
┌────────────────────▼──────────────────────────────────────────────┐
│                    DATABASE LAYER                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │           MongoDB (Port 27017)                           │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │                                                            │ │
│  │  Database: energyflow                                     │ │
│  │  │                                                        │ │
│  │  ├── Collections:                                         │ │
│  │  │   ├── users          (Authentication)                 │ │
│  │  │   ├── homes          (Properties)                     │ │
│  │  │   ├── devices        (Smart devices)                  │ │
│  │  │   ├── readings       (Energy data)                    │ │
│  │  │   ├── alerts         (Alert rules)                    │ │
│  │  │   ├── triggeredalerts(Fired alerts)                  │ │
│  │  │   └── devicetemplates(Device types)                  │ │
│  │  │                                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## 🔗 Request-Response Flow

### Example: User Login

```
1. User Types Email & Password
   ↓
2. Frontend: authService.login(email, password)
   ↓
3. Axios makes POST request to http://localhost:3001/api/auth/login
   {
     "email": "john.smith@example.com",
     "password": "password123"
   }
   ↓
4. Backend Route Handler: POST /api/auth/login
   ↓
5. Controller: authController.login()
   ├─ Find user by email
   ├─ Compare password hash
   ├─ Generate JWT token
   └─ Return token
   ↓
6. Response:
   {
     "success": true,
     "token": "eyJhbGc...",
     "user": { ... }
   }
   ↓
7. Frontend receives response
   ├─ Store token in localStorage
   ├─ Update Auth Context
   └─ Redirect to Dashboard
   ↓
8. All future requests include token:
   Authorization: Bearer eyJhbGc...
```

### Example: Get Homes

```
1. User clicks "Homes" link
   ↓
2. Frontend: useQuery('homes', homeService.getHomes)
   ↓
3. React Query triggers: GET /api/homes
   with header: Authorization: Bearer <token>
   ↓
4. Backend Auth Middleware: Verifies JWT token
   ✓ Valid → Continue
   ✗ Invalid → Return 401 Unauthorized
   ↓
5. Backend Route: GET /api/homes
   ↓
6. Controller: homeController.getHomes()
   ├─ Query: db.homes.find({ user: userId })
   ├─ Process results
   └─ Return homes array
   ↓
7. Response:
   {
     "success": true,
     "data": [
       { name: "Main Residence", ... },
       { name: "Summer House", ... }
     ]
   }
   ↓
8. Frontend receives
   ├─ React Query caches data
   ├─ Component re-renders
   └─ Display homes to user
```

## 📊 Authentication Flow (JWT)

```
Initial Login
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend              Backend              Database
   │                    │                     │
   │──Login Request──────>                    │
   │  {email, password}  │                    │
   │                     │──Find User────────>│
   │                     │<──User Object──────│
   │                     │                    │
   │                     ├─Verify Password    │
   │                     │ ✓ Match            │
   │                     │                    │
   │                     ├─Generate JWT       │
   │                     │  {id, role, exp}   │
   │                     │                    │
   │<──Token Response─────                    │
   │  {token, user}      │                    │
   │                     │                    │
   ├─Store in LocalStorage
   │ Authorization: Bearer <token>
   │
   
Subsequent Requests
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend              Backend              Database
   │                    │                     │
   │──API Request───────>                    │
   │ + Token Header     │                    │
   │                    ├─Verify JWT Token   │
   │                    │  ✓ Valid, Not Exp  │
   │                    │                    │
   │                    ├─Extract User ID    │
   │                    │                    │
   │                    ├─Query with UserID─>│
   │                    │<──Data──────────────│
   │                    │                    │
   │<──Response─────────                     │
   │  {success, data}   │                    │
   │                    │                    │

Token Expiration
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend              Backend
   │                    │
   │──API Request──────>│
   │ + Expired Token    │
   │                    │
   │                    ├─Verify JWT Token
   │                    │  ✗ Token Expired
   │                    │
   │<──401 Response─────│
   │  Unauthorized      │
   │                    │
   ├─Clear LocalStorage │
   ├─Clear Auth Context │
   └─Redirect to Login  │
```

## 🔐 Security Features

```
┌─────────────────────────────────────────┐
│        Security Layers                  │
├─────────────────────────────────────────┤
│                                         │
│ 1. HTTPS (In Production)                │
│    └─ Encrypt data in transit           │
│                                         │
│ 2. CORS Validation                      │
│    └─ Only localhost:5173 allowed       │
│                                         │
│ 3. JWT Authentication                   │
│    └─ Token-based, stateless            │
│                                         │
│ 4. Password Hashing (bcrypt)            │
│    └─ Passwords hashed, never stored    │
│                                         │
│ 5. Request Validation                   │
│    └─ Zod schemas validate input        │
│                                         │
│ 6. Rate Limiting                        │
│    └─ Prevent brute force attacks       │
│                                         │
│ 7. Error Handling                       │
│    └─ No sensitive data in errors       │
│                                         │
└─────────────────────────────────────────┘
```

## 📦 Component Communication

```
React Components
      ↓
  useAuth Hook
  useQuery Hook
      ↓
  Auth Context
  React Query
      ↓
  Service Layer
(homeService, deviceService, etc.)
      ↓
  API Layer
(api.js with Axios)
      ↓
  HTTP Requests
      ↓
  Backend Express Routes
      ↓
  Controllers & Models
      ↓
  MongoDB Database
      ↓
  Response ← Response ← Response ← Response ← Response
```

## 🚀 Deployment Architecture (Future)

```
┌──────────────────────────────────────────────────────┐
│           Production Deployment                      │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────────┐         ┌────────────────┐     │
│  │ Vercel/Netlify│         │ Heroku/Render  │     │
│  │ (Frontend)    │         │ (Backend)      │     │
│  │ Port: 443     │         │ Port: 443      │     │
│  └────────────────┘         └────────────────┘     │
│         ↓                          ↓                │
│  ┌────────────────┐         ┌────────────────┐     │
│  │ React Build    │         │ Node Cluster   │     │
│  │ (Optimized)    │         │ (Load Balanced)│     │
│  └────────────────┘         └────────────────┘     │
│         │                          │                │
│         └──────────┬───────────────┘                │
│                    │                                │
│            ┌───────▼────────┐                      │
│            │  MongoDB Atlas │                      │
│            │ (Cloud Database)                      │
│            └────────────────┘                      │
│                                                      │
└──────────────────────────────────────────────────────┘
```

## 🎯 Key Takeaways

1. **Frontend** (React) runs on `localhost:5173`
2. **Backend** (Express) runs on `localhost:3001`
3. **Database** (MongoDB) runs on `localhost:27017`
4. **Communication** is via HTTP/REST API with JWT authentication
5. **Frontend** manages UI state with React Context + TanStack Query
6. **Backend** handles business logic, validation, and database operations
7. **Security** ensured via JWT, password hashing, CORS, and input validation

---

**Architecture Type:** Monolithic (can be split into microservices later)
**Communication:** REST API with JSON
**Authentication:** JWT (JSON Web Tokens)
**Database:** MongoDB (NoSQL)
**Status:** ✅ Fully Connected
